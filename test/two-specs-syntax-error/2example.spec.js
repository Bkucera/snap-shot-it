// const snapshot = require('snap-shot-it')

it('errors', ()=>{
	console.log('testing....')
	
	// await new Promise((res)=>setTimeout(res,1000))
	// throw new Error('asdf')
	// done(new Error('sdf'))
})
